<!doctype html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <title>Hello {{name}}</title>
  </head>
  <body>
    <h1>Hello {{name}}</h1>
    <p>You can use <pre>{{token}}</pre> to log in to <a href="http://{{vault_address}}" target="_blank">Vault</a>.</p>
  </body>
</html>
<!doctype html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <title>Hello {{name}}</title>
      <style>
      pre {
        display: inline-block;
        padding: 5px;
        background-color: #eee;
        border-radius: 3px;
      }
      </style>
  </head>
  <body>
    <h1>Hello {{name}}</h1>
    <p>You can use <pre>{{username}}</pre> and <pre>{{password}}</pre> to log in to Grafana.</p>
  </body>
</html>